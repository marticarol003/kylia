{
  "scripts": {
    "test": "for f in tests/test-*.mjs; do echo \"── $f\"; node \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "puppeteer": "^24.43.0"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
